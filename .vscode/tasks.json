{
    "tasks": [
        {
            "label": "build tag-mapper-replay",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target tag-mapper-replay",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": false
            }
        },
        {
            "label": "build constrained_solvepnp_test",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target constrained_solvepnp_test",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
