{
    "tasks": [
        {
            "label": "build tag-mapper",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target tag-mapper",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}